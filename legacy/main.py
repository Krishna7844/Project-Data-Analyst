import pandas as pd
import os
from csv_preprocessing import Preprocessing

################################################################################################################################################################################################################################################################################################################################################################################

#Loading and Preprocessing the CSV's 
folder_path = input("Enter the path of the folder: ") # Taking folder path input
preprocess = Preprocessing(folder_path)
csv_files = preprocess.get_csv_files() # Collecting CSV files
print("CSV Files Found:", csv_files)
dfs = preprocess.load_dataframes()# Load data
print("Number of DataFrames loaded:", len(dfs))

################################################################################################################################################################################################################################################################################################################################################################################

